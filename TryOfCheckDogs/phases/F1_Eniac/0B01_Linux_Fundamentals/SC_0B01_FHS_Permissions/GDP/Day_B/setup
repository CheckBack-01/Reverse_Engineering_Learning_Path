#!/usr/bin/env bash

mkdir -p /tmp/day_b_evidence
cd /tmp/day_b_evidence

cat > timed_redrills.txt << 'EOF' 
# re-drills day b - timed
# format: drills_id | goal_time_seg | real_time_seg | result
EOF

